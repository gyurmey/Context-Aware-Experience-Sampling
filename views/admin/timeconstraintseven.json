{   "$ref": "constraint.json",
    "properties": {
        "type": {
            "type": "string",
            "enum": ["time"]
        },
        
        "userCanSet": {
            "type": "string",
            "enum": ["weekdays", "allDays", "weekends"]

        }
    }

}